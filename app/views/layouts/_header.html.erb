<header class="navbar navbar-default" role="navigation">
  <div class="navbar-header">
    <a href="#" class="navbar-brand">Viddit</a>
  </div>
  <% if signed_in? %>
    <%= render 'shared/video_form' %>
  <% end %>
</header>